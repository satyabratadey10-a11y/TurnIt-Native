name: MyCursurAI Part1 Baseline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept licenses
        run: yes | sdkmanager --licenses || true

      - name: Install SDK packages
        run: sdkmanager "platforms;android-34" "build-tools;34.0.0" "platform-tools"

      - name: Create directory tree
        run: |
          mkdir -p MyCursurAI/app/src/main/java/com/mycursurai/chat
          mkdir -p MyCursurAI/app/src/main/res/layout
          mkdir -p MyCursurAI/app/src/main/res/values
          mkdir -p MyCursurAI/gradle/wrapper

      - name: Write settings.gradle
        run: |
          cat > MyCursurAI/settings.gradle <<'SETTINGSGRADLEEOF'
pluginManagement {
repositories {
google()
mavenCentral()
gradlePluginPortal()
}
}
dependencyResolutionManagement {
repositoriesMode = RepositoriesMode.FAIL_ON_PROJECT_REPOS
repositories {
google()
mavenCentral()
}
}
rootProject.name = "MyCursurAI"
include(":app")
SETTINGSGRADLEEOF

      - name: Write root build.gradle
        run: |
          cat > MyCursurAI/build.gradle <<'ROOTBUILDEOF'
plugins {
id("com.android.application") version "8.7.3" apply false
}
ROOTBUILDEOF

      - name: Write app build.gradle
        run: |
          cat > MyCursurAI/app/build.gradle <<'APPBUILDEOF'
plugins {
id("com.android.application")
}
android {
namespace = "com.mycursurai.chat"
compileSdk = 34
defaultConfig {
applicationId = "com.mycursurai.chat"
minSdk = 26
targetSdk = 34
versionCode = 1
versionName = "1.0"
testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
}
buildTypes {
release {
minifyEnabled = false
proguardFiles(
getDefaultProguardFile("proguard-android-optimize.txt"),
"proguard-rules.pro"
)
}
}
compileOptions {
sourceCompatibility = JavaVersion.VERSION_21
targetCompatibility = JavaVersion.VERSION_21
}
}
dependencies {
implementation("androidx.appcompat:appcompat:1.7.0")
implementation("com.google.android.material:material:1.12.0")
implementation("androidx.constraintlayout:constraintlayout:2.1.4")
implementation("androidx.core:core:1.16.0")
testImplementation("junit:junit:4.13.2")
androidTestImplementation("androidx.test.ext:junit:1.2.1")
androidTestImplementation("androidx.test.espresso:espresso-core:3.6.1")
}
APPBUILDEOF

      - name: Write gradle-wrapper.properties
        run: |
          cat > MyCursurAI/gradle/wrapper/gradle-wrapper.properties <<'WRAPPEREOF'
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-9.3.1-bin.zip
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
WRAPPEREOF

      - name: Write gradle.properties
        run: |
          cat > MyCursurAI/gradle.properties <<'GRADLEPROPSEOF'
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
org.gradle.configuration-cache=false
GRADLEPROPSEOF

      - name: Write AndroidManifest.xml
        run: |
          cat > MyCursurAI/app/src/main/AndroidManifest.xml <<'MANIFESTEOF'
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<application
android:allowBackup="true"
android:icon="@mipmap/ic_launcher"
android:label="@string/app_name"
android:roundIcon="@mipmap/ic_launcher_round"
android:supportsRtl="true"
android:theme="@style/Theme.MyCursurAI">
<activity
android:name=".MainActivity"
android:exported="true">
<intent-filter>
<action android:name="android.intent.action.MAIN" />
<category android:name="android.intent.category.LAUNCHER" />
</intent-filter>
</activity>
</application>
</manifest>
MANIFESTEOF

      - name: Write colors.xml
        run: |
          cat > MyCursurAI/app/src/main/res/values/colors.xml <<'COLORSEOF'
<?xml version="1.0" encoding="utf-8"?>
<resources>
<color name="color_primary">#FF6200EE</color>
<color name="color_primary_dark">#FF3700B3</color>
<color name="color_accent">#FF03DAC5</color>
<color name="color_background">#FF121212</color>
<color name="color_surface">#FF1E1E1E</color>
<color name="color_on_primary">#FFFFFFFF</color>
<color name="color_on_surface">#FFFFFFFF</color>
<color name="color_text_secondary">#FF888888</color>
</resources>
COLORSEOF

      - name: Write strings.xml
        run: |
          cat > MyCursurAI/app/src/main/res/values/strings.xml <<'STRINGSEOF'
<?xml version="1.0" encoding="utf-8"?>
<resources>
<string name="app_name">MyCursurAI</string>
<string name="hello_message">MyCursurAI is ready.</string>
<string name="build_status">Part 1 Baseline — Build Successful</string>
</resources>
STRINGSEOF

      - name: Write themes.xml
        run: |
          cat > MyCursurAI/app/src/main/res/values/themes.xml <<'THEMESEOF'
<?xml version="1.0" encoding="utf-8"?>
<resources>
<style name="Theme.MyCursurAI" parent="Theme.Material3.Dark.NoActionBar">
<item name="colorPrimary">@color/color_primary</item>
<item name="colorPrimaryVariant">@color/color_primary_dark</item>
<item name="colorOnPrimary">@color/color_on_primary</item>
<item name="colorSecondary">@color/color_accent</item>
<item name="android:colorBackground">@color/color_background</item>
<item name="android:statusBarColor">@color/color_background</item>
<item name="android:navigationBarColor">@color/color_background</item>
<item name="android:windowLightStatusBar">false</item>
</style>
</resources>
THEMESEOF

      - name: Write activity_main.xml
        run: |
          cat > MyCursurAI/app/src/main/res/layout/activity_main.xml <<'ACTIVITYMAINEOF'
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
xmlns:android="http://schemas.android.com/apk/res/android"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:orientation="vertical"
android:gravity="center"
android:background="@color/color_background"
android:padding="32dp">
<TextView
android:id="@+id/tv_title"
android:layout_width="wrap_content"
android:layout_height="wrap_content"
android:text="@string/app_name"
android:textColor="@color/color_on_surface"
android:textSize="32sp"
android:textStyle="bold"
android:layout_marginBottom="16dp" />
<TextView
android:id="@+id/tv_status"
android:layout_width="wrap_content"
android:layout_height="wrap_content"
android:text="@string/build_status"
android:textColor="@color/color_text_secondary"
android:textSize="14sp"
android:gravity="center" />
</LinearLayout>
ACTIVITYMAINEOF

      - name: Write MainActivity.java
        run: |
          cat > MyCursurAI/app/src/main/java/com/mycursurai/chat/MainActivity.java <<'MAINACTIVITYEOF'
package com.mycursurai.chat;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {

@Override
protected void onCreate(Bundle savedInstanceState) {
super.onCreate(savedInstanceState);
setContentView(R.layout.activity_main);
}
}
MAINACTIVITYEOF

      - name: Write proguard-rules.pro
        run: |
          cat > MyCursurAI/app/proguard-rules.pro <<'PROGUARDEOF'
-keep class com.mycursurai.chat.** { *; }
-keepattributes *Annotation*
PROGUARDEOF

      - name: Bootstrap Gradle wrapper jar
        run: |
          curl -sL \
          "https://raw.githubusercontent.com/gradle/gradle/v8.6.0/gradle/wrapper/gradle-wrapper.jar" \
          -o MyCursurAI/gradle/wrapper/gradle-wrapper.jar \
          || wget -q \
          "https://raw.githubusercontent.com/gradle/gradle/v8.6.0/gradle/wrapper/gradle-wrapper.jar" \
          -O MyCursurAI/gradle/wrapper/gradle-wrapper.jar

      - name: Write gradlew
        run: |
          cat > MyCursurAI/gradlew <<'GRADLEWEOF'
#!/bin/sh
APP_HOME=$(dirname "$0")
APP_HOME=$(cd "$APP_HOME" && pwd)
CLASSPATH="$APP_HOME/gradle/wrapper/gradle-wrapper.jar"
exec "${JAVA_HOME}/bin/java" \
-classpath "$CLASSPATH" \
org.gradle.wrapper.GradleWrapperMain \
"$@"
GRADLEWEOF
          chmod +x MyCursurAI/gradlew

      - name: Sanitize all files
        run: |
          find MyCursurAI -type f \( \
          -name "*.gradle" -o \
          -name "*.xml" -o \
          -name "*.java" -o \
          -name "*.properties" -o \
          -name "*.pro" \
          \) | while read f; do
          LC_ALL=C sed -i 's/[^\x00-\x7F]//g' "$f"
          sed -i 's/\r//' "$f"
          done

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('MyCursurAI/**/*.gradle','MyCursurAI/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      - name: Run assembleDebug
        working-directory: MyCursurAI
        run: |
          ./gradlew assembleDebug \
          --no-daemon \
          --stacktrace \
          -Dorg.gradle.jvmargs="-Xmx2g" \
          2>&1 | tee build_output.log

      - name: Evaluate result
        working-directory: MyCursurAI
        run: |
          if grep -q "BUILD SUCCESSFUL" build_output.log; then
          echo "========================================"
          echo "  BUILD SUCCESSFUL — Green Checkmark"
          echo "========================================"
          exit 0
          fi
          echo "BUILD FAILED — last 60 lines:"
          tail -60 build_output.log
          exit 1

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: MyCursurAI-Part1-debug
          path: MyCursurAI/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 14

      - name: Upload failure logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-failure-logs
          path: MyCursurAI/build_output.log
          retention-days: 7
